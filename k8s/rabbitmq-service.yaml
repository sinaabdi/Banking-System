apiVersion: v1
kind: Service
metadata:
  namespace: banking
  name: rabbitmq
spec:
  selector:
    app: rabbitmq
  ports:
    - port: 15672
      targetPort: 15672
      name: web-dashboard
    - port: 5672
      targetPort: 5672
      name: client-traffic    
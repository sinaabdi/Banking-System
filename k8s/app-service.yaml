apiVersion: v1
kind: Service
metadata:
  namespace: banking
  name: core
spec:
  type: NodePort
  selector:
    app: core
  ports:
    - port: 8080
      targetPort: 8080
      nodePort: 30080